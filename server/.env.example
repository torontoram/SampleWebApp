# Example env file for the contact server
# Copy to .env and fill with real values

# SMTP server settings (required)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password

# Optional: from address used when sending mail (defaults to SMTP_USER)
FROM_EMAIL=sender@example.com

# Recipient for contact messages (defaults to spbluecorp@gmail.com)
CONTACT_RECIPIENT=spbluecorp@gmail.com

# Server port
PORT=3000
